# Factorial Calculator

This program defines a function called `factorial_number()` that takes a positive integer and returns its factorial. The factorial of a number `n` is the product of all positive integers from `1` to `n`.

## How to Run

```bash
python3 factorial.py
